{% include ['header/header.html.twig', 'base.html.twig'] %}

{% block title %}{% endblock %}

{% block body %}
<body style="background-color: #EAEDED ">
  <div class="">
  <div class="row">
    <div class="col-2 text-white rounded" style="background-color: #34568B">
      <div> <img style="max-width : 200px;" class="rounded-circle shadow mt-4 mx-auto d-block" src="{{ vich_uploader_asset(app.user, 'imageFile') }}"></div>
     <h5 class="m-4 text-center"> {{app.user.firstName }} {{app.user.lastName}}</h5>
     <div class="m-4"><h4>Bio </h4> <small>{{app.user.bio}}</small></div>
     <div class="ms-4 d-flex"><h6>Email </h5> : {{app.user.email}}</div> 
     <div class="ms-4 d-flex"><h6>Address </h5> : {{app.user.address}}</div>
     <div class="ms-4 d-flex"><h6>Ago </h5> : {{app.user.createdAT|date('Y-m-d H:i')}}</div>
     <h6 class="ms-4 mt-4 d-flex">  <a class="nav-link active" href="{{ path('app_login') }}"> Edit Profil</a> </h6>
     <h6 class="ms-4 mt-2 mb-2 d-flex">  <a class="nav-link active" href="{{ path('app_login') }}">  Edit Password</a> </h6>
      <h6 class="ms-4 mt-2 mb-2 d-flex">  <a class="nav-link active" href="{{ path('app_login') }}">My Article</a> </h6>
       <h6 class="ms-4 mt-2 mb-2 d-flex">  <a class="nav-link active" href="{{ path('app_login') }}">Create Article</a> </h6>
     <h6 class="ms-4 border-top mt-2 mb-4 pt-2 d-flex">  <a class="nav-link active" href="{{ path('app_login') }}">Logout</a> </h6>
    </div>
    <div class="col-7">
      <div class="m-4 d-flex">
         <img style="max-width : 70px;" class="rounded-circle shadow mt-4  d-block" src="{{ vich_uploader_asset(app.user, 'imageFile') }}">
         <div class=" ms-4" style="margin-top : 30px;"> <h5> Profil </h5> <p >Update Your Photo and personal details</p></div>
         
      </div>
      <div class="m-4 d-flex">
         <h6 class="mt-2 ">firstName  </h6>
         <p class="form-control w-50 ms-2 shadow"> {{app.user.firstName }}</p>
      </div>
      <div class="m-4 d-flex">
         <h6 class="mt-2 ">lastName   </h6>
         <p class="form-control w-50 ms-2 shadow "> {{app.user.lastName}}</p>
      </div>
       <div class="m-4 d-flex">
         <h6 class="mt-2 me-4">Bio</h6>
         <p class="form-control w-50 ms-4 shadow" >  {{app.user.bio}}</p>
      </div>
      <div class="m-4 d-flex">
         <h6 class="mt-2 me-1">Address</h6>
         <p class="form-control w-50 ms-3 shadow"> {{app.user.address}}</p>
      </div>
      <div class="m-4 d-flex">
         <h6 class="mt-2 me-4"> email</h6>
         <p class="form-control w-50 ms-3 shadow"> {{app.user.email}}</p>
         <div class="">
         <a class="btn btn-info ms-2 me-2 p-1" href="{{ path('edit_user', { id: app.user.id } ) }}">Edit Profil</a>
         <a class="btn btn-info ms-2 me-2 p-1" href="{{ path('app_edit.password', { id: app.user.id } ) }}" >Edit Password</a>
         <a class="btn btn-danger p-1" href="{{ path('user_delete', { id: app.user.id } ) }}">Delete Profil</a>
         </div>
         
      </div>
      
    </div>
    <div class="col-3">
    <diV class="container m-4">
          <a class="btn btn-primary mt-4">Create New Article</a>
       </div>
       <div class="container m-4">
       <a class="btn btn-primary">Show My Articles</a>
      </div>
      <div class="container m-4">
       <a class="btn btn-warning">Contact Us</a>
      </div>
      </div>
  </div>
  
</div>

</body>
{% endblock %}
{% include 'footer/footer.html.twig' %}